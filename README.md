# certinext-zabbix

CertiNext DCV monitoring for Zabbix: import template + trapper pusher.

Content is being extracted from `ums-certinext-scripts` (an internal,
University of Maine System-only repository, not publicly accessible) in
phases. This README will be replaced with the full public-facing version in
Phase 4.
